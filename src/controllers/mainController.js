const {registerUser,getUser,upload, deleteUser, allUserDelete, seletedDelete, updateUser, getSingleUser, searchUserByName } = require("./formController/formController");

module.exports={
    registerUser,
    getUser,
    deleteUser,
    upload,
    allUserDelete,
    seletedDelete,
    updateUser,
    getSingleUser,
    searchUserByName
    
}